<?xml version="1.0"?>
<!--
/**
 * Process Eight
 *
 * DISCLAIMER
 *
 * Do not edit or add to this file if you wish to upgrade this module to newer
 * versions in the future. If you wish to customize this module for your
 * needs please contact ProcessEight for more information.
 *
 * @package     m23-example-modules
 * @copyright   Copyright (c) 2019 Process Eight
 * @author      Process Eight
 *
 */
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:module:Magento_Backend:etc/menu.xsd">
    <menu>
        <!--
        /**
         * Node:
         * add: Action designed for adding new item to existing items as a child
         *
         * Attributes:
         * id: Required. Item id attribute can has only [a-z0-9/_]. Minimal length 3 symbol. Case insensitive. Must be unique.
         * title: Required. Minimal length 3 symbol.
         * module: Required. Value must match /[A-Za-z0-9_]{3,}/. Item module attribute can has only [a-z0-9_]. Minimal length 3 symbol. Case insensitive.
         * resource: Required. Defines a matching resource in acl.xml. Value must match /[A-Z]+[A-Za-z0-9]{1,}_[A-Z]+[A-Z0-9a-z]{1,}::[A-Za-z_0-9]{1,}/. Minimal length 3 symbol. Case insensitive.
         * sortOrder: Optional. Defines sort order in menu. Int.
         * action: Optional. URI to an admin page. Item action attribute can has only [a-zA-Z0-9/_]. Minimal length 3 symbol.
         * parent: Optional. Item id attribute can has only [a-z0-9/_]. Minimal length 3 symbol. Case insensitive. Must be unique?
         * toolTip: Optional. Text which appears which user hovers over menu item. Minimal length 3 symbol.
         * dependsOnModule: Optional. Value must match /[A-Za-z0-9_]{3,}/. Minimal length 3 symbol. Case insensitive.
         * dependsOnConfig: Optional. Value must match /[A-Za-z0-9_/]{3,}/. Minimal length 3 symbol. Case insensitive.
         * translate: Optional. Defines strings which will be translated. String.
         * target: Optional. Forces the action to open in a new window and appends the 'Open in new window' icon to the title. String.
         -->
        <add id="ProcessEight_AddCustomAdminhtmlRouterExample::listing"
             title="Add Custom Adminhtml Router Example"
             module="ProcessEight_AddCustomAdminhtmlRouterExample"
             resource="ProcessEight_AddCustomAdminhtmlRouterExample::index"
             sortOrder="100"
             parent="ProcessEight_Shared::extensions"
             action="addcustomadminhtmlrouterexample/example/index"
             target="_blank"
             translate="title"/>
    </menu>
</config>